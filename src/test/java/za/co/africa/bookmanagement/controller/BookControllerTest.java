package za.co.africa.bookmanagement.controller;

/*import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.web.servlet.MockMvc;

import za.co.africa.bookmanagement.service.BookService;
*/
/*import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.web.servlet.MockMvc;

import za.co.africa.bookmanagement.service.BookService;
*/
/*@RunWith(SpringRunner.class)
@WebMvcTest(value= BookController.class, secure = false)*/
public class BookControllerTest {
	
	/*@Autowired
	private MockMvc mockMvc;
	
	@MockBean
	private BookService bookService;*/
	
	/*@Test
	public void createBookTest() {
		List<Book> books = new ArrayList<>();
		Address address = new Address(1, "Joburg", "sandton", "2196");
		Library library = new Library(1, "Capital",books , address);
		List<Book> bookss = Arrays.asList(
				new Book(1, "Java", "abc", library),
				new Book(2, "Spring", "xyz", library),
				new Book(3, "Angular", "def", library));
		Book book = new Book(1, "Java", "abc", library);
		String exampleBook = "{\"author\": \"string\", \"library\": {\"address\": {\"city\": \"string\",\"state\": \"string\",\"zip_Code\": \"string\"
			      			},
			      			\"books\": [
			      				{}
			      			],
			      			\"library_id\": 0,
			      			\"name\": \"string\"
			    },
			    \"name\": \"string\"
			  }";
		Mockito.when(bookService.createBook(book));
				
	}*/

}
